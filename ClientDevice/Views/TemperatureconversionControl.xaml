<UserControl x:Class="ClientDevice.Views.TemperatureConversionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClientDevice.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="440">
    
    <Border Grid.Row="2" BorderThickness="0,0,0,2" BorderBrush="Gray" Margin="5">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--    Title   -->
            <TextBlock Text="Temperature Conversion" FontSize="18" FontWeight="SemiBold" 
                       Foreground="#D9D9D9" Margin="2.5" Grid.Column="0" Grid.Row="0" />

            <TextBlock Text="Please enter the temperature in degrees Celsius" FontSize="12" Margin="2.5"
                   Foreground="#D9D9D9" Grid.Column="0" Grid.Row="1" />

            <TextBox Grid.Row="2" Grid.Column="0" Margin="2.5" />

            <Button Content="Calculate" Grid.Column="2" Grid.Row="2" 
                    Margin="2.5" Padding="2.5" />
        </Grid>
    </Border>
</UserControl>